



//-------------删除字符串中的空格，空间复杂度常数级---------------

void delet_space(char *str) 
{
    int i = 0;           //i下标始终指向下一个可用的空位
    char * p = str;      //p指针向后扫描
    
    while(*p)
    {
        if(*p != ' ')    //遇到空格时，i不动，p继续走。
            str[i++] = *p; 
        p++;
    } 
    str[i]=‘\0’; 
}
//-----------------end--------------------------------------